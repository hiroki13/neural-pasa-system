class Sample(object):

    def __init__(self, word_ids, tag_ids, prd_index, context):
        self.word_ids = word_ids
        self.tag_ids = tag_ids
        self.prd_index = prd_index
        self.context = context

